apiVersion: v2
name: webhook-listener
version: 1.0.0
description: A Helm chart for Kubernetes deployment of a webhook listener

# This is the version number of the application being deployed
appVersion: "1.0.0"

# Dependencies
# No dependencies for this simple app

# Chart maintainers
maintainers:
  - name: DevOps GPT
    email: devopsgpt@example.com

# Chart dependencies
# dependencies:
#   - name: some-dependency
#     version: "1.0.0"
#     repository: "@local"